using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine.UI;

public class FieldManager : MonoBehaviour
{
    /*public DeckManager deckManager;
    public GameObject cardPrefab;
    public List<Transform> fieldSlots;  // Lista degli slot nel campo
    public List<GameObject> cardsInField = new List<GameObject>();  // Carte nel campo

    void Start()
    {
    }

    public void AddCardToField(Card cardData, Transform targetSlot)
    {
        GameObject newCard = Instantiate(cardPrefab, targetSlot.position, Quaternion.identity, targetSlot);
        cardsInField.Add(newCard);
        newCard.GetComponent<CardDisplay>().cardData = cardData;
    }

    public void RemoveCardFromField(GameObject card)
    {
        if (cardsInField.Contains(card))
        {
            cardsInField.Remove(card);
            Destroy(card);
        }
    }

    void Update()
    {
    }*/
}



